services:

  social-graph-service:
    image: deathstarbench/social-network-microservices:latest
    container_name: ${TJOB_NAME}-social-graph-service
    hostname: social-graph-service
    depends_on:
      - jaeger-agent
      - social-graph-mongodb
    restart: always
    entrypoint: SocialGraphService
    volumes:
      - ./sut/config:/social-network-microservices/config
    networks:
      - jenkins_network

  social-graph-mongodb:
    image: mongo:4.4.6
    container_name: ${TJOB_NAME}-social-graph-mongodb
    hostname: social-graph-mongodb
    restart: always
    networks:
      - jenkins_network

  social-graph-redis:
    image: redis
    container_name: ${TJOB_NAME}-social-graph-redis
    hostname: social-graph-redis
    restart: always
    networks:
      - jenkins_network

  home-timeline-redis:
    image: redis
    container_name: ${TJOB_NAME}-home-timeline-redis
    hostname: home-timeline-redis
    restart: always
    networks:
      - jenkins_network

  compose-post-service:
    image: deathstarbench/social-network-microservices:latest
    container_name: ${TJOB_NAME}-compose-post-service
    hostname: compose-post-service
    restart: always
    depends_on:
      - jaeger-agent
    entrypoint: ComposePostService
    volumes:
      - ./sut/config:/social-network-microservices/config
    networks:
      - jenkins_network

  compose-post-redis:
    image: redis
    container_name: ${TJOB_NAME}-compose-post-redis
    hostname: compose-post-redis
    restart: always
    networks:
      - jenkins_network

  post-storage-service:
    image: deathstarbench/social-network-microservices:latest
    container_name: ${TJOB_NAME}-post-storage-service
    hostname: post-storage-service
    restart: always
    depends_on:
      - jaeger-agent
      - post-storage-mongodb
    entrypoint: PostStorageService
    volumes:
      - ./sut/config:/social-network-microservices/config
    networks:
      - jenkins_network

  post-storage-memcached:
    image: memcached
    container_name: ${TJOB_NAME}-post-storage-memcached
    hostname: post-storage-memcached
    restart: always
    networks:
      - jenkins_network

  post-storage-mongodb:
    image: mongo:4.4.6
    container_name: ${TJOB_NAME}-post-storage-mongodb
    hostname: post-storage-mongodb
    restart: always
    networks:
      - jenkins_network

  user-timeline-service:
    image: deathstarbench/social-network-microservices:latest
    container_name: ${TJOB_NAME}-user-timeline-service
    hostname: user-timeline-service
    restart: always
    depends_on:
      - jaeger-agent
      - user-timeline-mongodb
    entrypoint: UserTimelineService
    volumes:
      - ./sut/config:/social-network-microservices/config
    networks:
      - jenkins_network

  user-timeline-redis:
    image: redis
    container_name: ${TJOB_NAME}-user-timeline-redis
    hostname: user-timeline-redis
    restart: always
    networks:
      - jenkins_network

  user-timeline-mongodb:
    image: mongo:4.4.6
    container_name: ${TJOB_NAME}-user-timeline-mongodb
    hostname: user-timeline-mongodb
    restart: always
    networks:
      - jenkins_network

  url-shorten-service:
    image: deathstarbench/social-network-microservices:latest
    container_name: ${TJOB_NAME}-url-shorten-service
    hostname: url-shorten-service
    restart: always
    entrypoint: UrlShortenService
    depends_on:
      - jaeger-agent
      - url-shorten-mongodb
    volumes:
      - ./sut/config:/social-network-microservices/config
    networks:
      - jenkins_network

  url-shorten-memcached:
    image: memcached
    container_name: ${TJOB_NAME}-url-shorten-memcached
    hostname: url-shorten-memcached
    restart: always
    networks:
      - jenkins_network

  url-shorten-mongodb:
    image: mongo:4.4.6
    container_name: ${TJOB_NAME}-url-shorten-mongodb
    hostname: url-shorten-mongodb
    restart: always
    networks:
      - jenkins_network

  user-service:
    image: deathstarbench/social-network-microservices:latest
    container_name: ${TJOB_NAME}-user-service
    hostname: user-service
    restart: always
    depends_on:
      - jaeger-agent
      - user-mongodb
    entrypoint: UserService
    volumes:
      - ./sut/config:/social-network-microservices/config
    networks:
      - jenkins_network

  user-memcached:
    image: memcached
    container_name: ${TJOB_NAME}-user-memcached
    hostname: user-memcached
    restart: always
    networks:
      - jenkins_network

  user-mongodb:
    image: mongo:4.4.6
    container_name: ${TJOB_NAME}-user-mongodb
    hostname: user-mongodb
    restart: always
    networks:
      - jenkins_network

  media-service:
    image: deathstarbench/social-network-microservices:latest
    container_name: ${TJOB_NAME}-media-service
    hostname: media-service
    depends_on:
      - jaeger-agent
      - media-mongodb
    restart: always
    entrypoint: MediaService
    volumes:
      - ./sut/config:/social-network-microservices/config
    networks:
      - jenkins_network

  media-memcached:
    image: memcached
    container_name: ${TJOB_NAME}-media-memcached
    hostname: media-memcached
    restart: always
    networks:
      - jenkins_network

  media-mongodb:
    image: mongo:4.4.6
    container_name: ${TJOB_NAME}-media-mongodb
    hostname: media-mongodb
    restart: always
    networks:
      - jenkins_network

  text-service:
    image: deathstarbench/social-network-microservices:latest
    container_name: ${TJOB_NAME}-text-service
    hostname: text-service
    restart: always
    depends_on:
      - jaeger-agent
    entrypoint: TextService
    volumes:
      - ./sut/config:/social-network-microservices/config
    networks:
      - jenkins_network

  unique-id-service:
    image: deathstarbench/social-network-microservices:latest
    container_name: ${TJOB_NAME}-unique-id-service
    hostname: unique-id-service
    restart: always
    depends_on:
      - jaeger-agent
    entrypoint: UniqueIdService
    volumes:
      - ./sut/config:/social-network-microservices/config
    networks:
      - jenkins_network

  user-mention-service:
    image: deathstarbench/social-network-microservices:latest
    container_name: ${TJOB_NAME}-user-mention-service
    hostname: user-mention-service
    depends_on:
      - jaeger-agent
    restart: always
    entrypoint: UserMentionService
    volumes:
      - ./sut/config:/social-network-microservices/config
    networks:
      - jenkins_network

  home-timeline-service:
    image: deathstarbench/social-network-microservices:latest
    container_name: ${TJOB_NAME}-home-timeline-service
    hostname: home-timeline-service
    depends_on:
      - jaeger-agent
    restart: always
    entrypoint: HomeTimelineService
    volumes:
      - ./sut/config:/social-network-microservices/config
    networks:
      - jenkins_network

  write-home-timeline-service:
    image: deathstarbench/social-network-microservices:latest
    container_name: ${TJOB_NAME}-write-home-timeline-service
    hostname: write-home-timeline-service
    depends_on:
      - jaeger-agent
      - write-home-timeline-rabbitmq
    restart: always
    entrypoint: WriteHomeTimelineService
    volumes:
      - ./sut/config:/social-network-microservices/config
    networks:
      - jenkins_network

  write-home-timeline-rabbitmq:
    image: rabbitmq
    container_name: ${TJOB_NAME}-write-home-timeline-rabbitmq
    hostname: write-home-timeline-rabbitmq
    restart: always
    networks:
      - jenkins_network

  nginx-thrift:
    image: yg397/openresty-thrift:xenial
    container_name: ${TJOB_NAME}-nginx-thrift
    hostname: nginx-thrift
    ports:
      - ${frontend_port}:8080
    depends_on:
      - jaeger-agent
    restart: always
    volumes:
      - ./sut/nginx-web-server/lua-scripts:/usr/local/openresty/nginx/lua-scripts
      - ./sut/nginx-web-server/pages:/usr/local/openresty/nginx/pages
      - ./sut/nginx-web-server/conf/nginx.conf:/usr/local/openresty/nginx/conf/nginx.conf
      - ./sut/nginx-web-server/jaeger-config.json:/usr/local/openresty/nginx/jaeger-config.json
      - ./sut/gen-lua:/gen-lua
      - ./sut/docker/openresty-thrift/lua-thrift:/usr/local/openresty/lualib/thrift
    networks:
      - jenkins_network

  jaeger-agent:
    image: jaegertracing/all-in-one:latest
    container_name: ${TJOB_NAME}-jaeger-agent
    hostname: jaeger-agent
    restart: always
    environment:
      - COLLECTOR_ZIPKIN_HTTP_PORT=9411
    networks:
      - jenkins_network

networks:
  jenkins_network:
    external: true
